apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: build-shell
  labels:
    app: build-shell
spec:
  serviceName: build-shell
  selector:
    matchLabels:
      app: build-shell
  template:
    metadata:
      labels:
        app: build-shell
    spec:
      containers:
      - name: build-shell
        #image: docker:dind
        image: gcr.io/stardust-156404/build-pod:latest
        securityContext:
          privileged: true
      nodeSelector:
        cloud.google.com/gke-nodepool: build
      tolerations:
      - key: cloud.google.com/gke-preemptible
        operator: Equal
        value: "true"
        effect: NoSchedule
